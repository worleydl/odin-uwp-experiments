Experiments with odin on UWP.

Currently only showcasing how to setup raylib, run the `build.sh` script then setup the cmake project in the `uwp` folder.


## Limitations
- Currently only functioning on series hardware, I need to update some of the screen info methods in libuwp to work on windows.

---

All demonstration code is licensed under the unlicense.
